# Aegis dashboard configuration.

# Where the gateway is reachable from the browser. Must match the origin the gateway
# allows in CORS (AEGIS_APP_URL on the gateway side must point back here).
NEXT_PUBLIC_AEGIS_API_URL=http://localhost:8080

# Used for canonical URLs and Open Graph metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
